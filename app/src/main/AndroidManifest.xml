<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.lyf.customcontrol">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".pathtext02.quad.QuadPathActivity"></activity>
        <activity android:name=".pathtext02.arc.ArcPathActivity" />
        <activity android:name=".pathtext02.oval.OvalPathActivity" />
        <activity android:name=".pathtext02.circle.CirclePathActivity" />
        <activity android:name=".pathtext02.roundrect.RoundRectPathActivity" />
        <activity android:name=".pathtext02.rect.RectPathActivity" />
        <activity android:name=".pathtext02.line.LinePathActivity" />
        <activity android:name=".draw01.arc.ArcActivity" />
        <activity android:name=".draw01.oval.OvalActivity" />
        <activity android:name=".draw01.rect.RectActivity" />
        <activity android:name=".draw01.point.PointActivity" />
        <activity android:name=".draw01.line.LineActivity" />
        <activity android:name=".draw01.circle.CircleActivity" />
        <activity android:name=".main.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>